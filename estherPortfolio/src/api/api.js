import axios from 'axios';

/*const instance = axios.create({
    method: 'get',
    baseURL: 'https://cdn.contentful.com/api',
    headers: {
        'Authorization': `Bearer ${accessToken}`
    }
})

console.log(instance);*/
